# 8. Натуральное число, записанное в десятичной системе счисления, называется сверхпростым,
# если оно остается простым при любой перестановке своих цифр. Определить все сверхпростые числа до n.



def ex1(t):
    A = []
    for a in range (2,(t+1),1):
        t = 0
        r=0
        i = 0

        d = a % 10
        s = (a // 10)
        b = d*10 + s
        # print("Первое ",a,"второе ",b)

        for i in range (2,a,1):
            if (a%i==0):
                r=1
        for i in range (2,b,1):
            if (b%i==0):
                t=1
        if (t==0 and r==0):
            A.append(a)
    return A

# n=int(input("Введите предел (двухзначный), до которого числа определить сверхпростое число: \n"))

